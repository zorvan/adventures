(ns adventures.cljs.app
  (:require
   [adventures.cljs.reframe.core :refer [init re-render]
   ;[adventure.cljs.fulcro   :refer [init re-render]
   ]
   ))
   
   (defonce init-block (init))
   
   
